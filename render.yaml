services:
  - type: web
    name: smartwork-backend
    plan: free
    env: python
    healthCheckPath: /health
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.2
      # Chuỗi kết nối MongoDB — đặt thủ công trong bảng điều khiển Render,
      # không ghi vào tệp này
      - key: MONGO_URI
        sync: false
      - key: DB_NAME
        value: smartwork
      # Khoá ký thẻ truy cập. Render tự sinh; đổi khoá sẽ làm mọi phiên đăng
      # nhập hiện có hết hiệu lực
      - key: SECRET_KEY
        generateValue: true
      # Địa chỉ giao diện được phép gọi API. Bắt buộc đặt sau khi có tên miền
      # thật của bản triển khai giao diện, ví dụ:
      #   https://smartwork-ai.vercel.app
      - key: CORS_ORIGINS
        sync: false
      # Để "false" trên bản công khai. Dữ liệu mẫu tạo 64 tài khoản dùng chung
      # mật khẩu admin123 / 123456, trong đó có tài khoản quản trị — nếu cho
      # phép thì ai tìm ra địa chỉ cũng vào được với quyền cao nhất.
      # Chỉ đặt "true" khi chạy demo trong môi trường có kiểm soát.
      - key: ALLOW_DEMO_ACCOUNTS
        value: "false"
